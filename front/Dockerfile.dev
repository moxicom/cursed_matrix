# syntax=docker/dockerfile:1
# Vite dev server for docker-compose.dev.yml; source is bind-mounted at run time.
FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json .npmrc ./
RUN npm ci

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
